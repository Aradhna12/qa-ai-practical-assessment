apiVersion: v1
kind: Service
metadata:
  name: nginx-demo
  namespace: qa-demo
  labels:
    app: nginx-demo
spec:
  type: ClusterIP
  selector:
    app: nginx-demo
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
